# Conservative Gradle runtime settings for low-memory environments.
org.gradle.jvmargs=-Xmx512m -XX:MaxMetaspaceSize=256m -Dfile.encoding=UTF-8
org.gradle.parallel=false
org.gradle.daemon=false
org.gradle.workers.max=1

# Core mod coordinates
mod_id=bbs
mod_name=BBS FS mod
mod_license=MIT
mod_version=1.9
mod_group_id=mchorse
archives_base_name=bbs

# Platform versions (NeoForge 1.21.1)
minecraft_version=1.21.1
minecraft_version_range=[1.21.1]
java_version=21
neo_version=21.1.222
neo_version_range=[21.1,)
loader_version_range=[1,)

# Optional: parchment mappings (keep commented until needed)
# neogradle.subsystems.parchment.minecraftVersion=1.21.1
# neogradle.subsystems.parchment.mappingsVersion=2024.11.17
