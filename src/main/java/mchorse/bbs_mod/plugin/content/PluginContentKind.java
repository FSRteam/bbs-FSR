package mchorse.bbs_mod.plugin.content;

/**
 * Host-owned content projections supported by a content-only plugin snapshot.
 */
public enum PluginContentKind
{
    RESOURCE,
    LANGUAGE,
    SETTINGS,
    UI,
    DATA
}
