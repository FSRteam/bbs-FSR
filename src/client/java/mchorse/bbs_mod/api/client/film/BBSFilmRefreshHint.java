package mchorse.bbs_mod.api.client.film;

/** A stable hint for refreshing the native editor after a remote mutation. */
public enum BBSFilmRefreshHint
{
    NONE,
    VALUE,
    TIMELINE,
    REPLAY,
    STRUCTURE
}
